# Configuration Examples

This folder contains example configuration files for deploying the application:

* .env.example: Template for environment variables and API keys.
* docker-compose.yml: Standard Docker Compose setup for local Qdrant container.